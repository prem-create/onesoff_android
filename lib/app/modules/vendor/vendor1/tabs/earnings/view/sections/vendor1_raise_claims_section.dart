import 'package:flutter/material.dart';

class Vendor1RaiseClaimsSection extends StatelessWidget {
  const Vendor1RaiseClaimsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
